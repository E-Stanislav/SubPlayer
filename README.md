# SubPlayer

Кроссплатформенный офлайн-плеер с автоматической генерацией субтитров и переводом на русский язык.

## 🚀 Быстрый старт (один скрипт!)

**Linux / macOS:**
```bash
./setup.sh && ./run.sh
```

**Windows:**
```cmd
setup.bat
run.bat
```

Всё! Скрипт установки автоматически:
- Проверит Python
- Создаст виртуальное окружение
- Установит все зависимости
- Проверит системные утилиты (ffmpeg, mpv)

## Возможности

- Воспроизведение медиа файлов (mp4, mkv, avi, mov, mp3, wav, flac и др.)
- Автоматическая генерация субтитров через faster-whisper (офлайн, быстрее чем оригинальный Whisper)
- Перевод субтитров на русский язык через Argos Translate (офлайн)
- Кэширование результатов для быстрого доступа
- Portable версия без установки

## Требования

- **Python 3.9+** (проверяется автоматически скриптом установки)
- **FFmpeg** (для извлечения аудио) - опционально, можно установить позже
- **mpv/libmpv** (для воспроизведения) - опционально, python-mpv может работать без системного mpv
- **faster-whisper** - устанавливается автоматически

Скрипт установки проверит наличие всех зависимостей и подскажет, что нужно установить.

## Быстрый старт

### Linux / macOS

```bash
./setup.sh
./run.sh
```

### Windows

```cmd
setup.bat
run.bat
```

**Всё!** После выполнения скрипта установки приложение готово к использованию.

## Ручная установка (опционально)

Если вы предпочитаете ручную установку:

```bash
# Создание виртуального окружения (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -e .

# Запуск
python -m subplayer.app
```

## Структура проекта

- `subplayer/` - основной код приложения
  - `app.py` - точка входа
  - `ui/` - UI компоненты
  - `media/` - интеграция с mpv
  - `subs/` - генерация субтитров
  - `translate/` - перевод субтитров
  - `util/` - утилиты
- `packaging/` - файлы для сборки portable версии
- `tools/` - инструкции по включению внешних зависимостей

## Сборка portable версии

См. подробные инструкции в `packaging/README.md`

Краткая инструкция:
1. Установите зависимости: `pip install -e .`
2. Подготовьте бинарные файлы (mpv, ffmpeg) согласно инструкциям в `tools/`
3. (Опционально) Включите модели Whisper в сборку (см. `tools/bundle_fasterwhisper.md`)
4. Запустите сборку: `pyinstaller packaging/pyinstaller.spec`

**Примечание**: faster-whisper - это Python библиотека, она автоматически включается в сборку. Модели скачиваются автоматически при первом использовании или могут быть включены в сборку вручную.

## Использование

1. Откройте медиа файл через меню "Файл" → "Открыть файл..."
2. Нажмите "Сгенерировать субтитры" для создания субтитров (может занять время)
3. Нажмите "Перевести на русский" для перевода субтитров
4. Используйте настройки для изменения параметров (модель Whisper, язык и т.д.)

## Примечания

- Первый запуск перевода может потребовать установки языкового пакета Argos Translate (требуется интернет)
- Первый запуск генерации субтитров скачает модель Whisper автоматически (требуется интернет)
- Генерация субтитров требует значительных вычислительных ресурсов и может занять время
- Рекомендуется использовать модель "base" или "small" для баланса скорости и качества
- faster-whisper автоматически использует GPU (CUDA/ROCm) если доступно, что значительно ускоряет обработку