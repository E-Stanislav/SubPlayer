# SubPlayer

Простое кросс-платформенное приложение для воспроизведения видео и аудио файлов.

## Возможности

- 🎬 Воспроизведение видео (MP4, AVI, MOV, MKV, WebM)
- 🎵 Воспроизведение аудио (MP3, WAV, FLAC, OGG, M4A, AAC)
- 📝 Создание и редактирование субтитров (SRT формат)
- 🎯 Синхронизация субтитров с видео в реальном времени
- 📖 Загрузка существующих файлов субтитров
- 💾 Сохранение субтитров в файл
- 🎨 Современный и красивый интерфейс
- 🖱️ Поддержка перетаскивания файлов
- 💻 Работает на Windows, macOS и Linux

## Установка и запуск

### Требования

- Node.js (версия 16 или выше)
- npm или yarn

### Быстрая установка

#### Для macOS и Linux:
```bash
# Установка зависимостей
./install.sh

# Или вручную
npm install
```

#### Для Windows:
```batch
REM Установка зависимостей
install.bat

REM Или вручную
npm install
```

### Запуск приложения

```bash
npm start
# или
npm run dev
```

## Сборка для продакшена

Для удобства сборки созданы готовые скрипты, которые автоматически установят зависимости и соберут приложение.

### Сборка с помощью скриптов (рекомендуется)

#### macOS и Linux:

```bash
# Сборка для текущей платформы
./build.sh

# Сборка для Windows
./build.sh win

# Сборка для macOS
./build.sh mac

# Сборка для Linux
./build.sh linux

# Сборка для всех платформ
./build.sh all
```

#### Windows:

**Вариант 1: Batch скрипт (build.bat)**
```batch
REM Сборка для текущей платформы
build.bat

REM Сборка для Windows
build.bat win

REM Сборка для macOS
build.bat mac

REM Сборка для Linux
build.bat linux

REM Сборка для всех платформ
build.bat all
```

**Вариант 2: PowerShell скрипт (build.ps1)**
```powershell
# Сборка для текущей платформы
.\build.ps1

# Сборка для Windows
.\build.ps1 win

# Сборка для macOS
.\build.ps1 mac

# Сборка для Linux
.\build.ps1 linux

# Сборка для всех платформ
.\build.ps1 all
```

### Сборка с помощью npm команд

Если скрипты недоступны, используйте npm команды:

```bash
# Установка зависимостей (если еще не установлены)
npm install

# Сборка для текущей платформы
npm run build

# Сборка для конкретной платформы
npm run build:win    # Windows
npm run build:mac    # macOS
npm run build:linux  # Linux
npm run build:all    # Все платформы

# Очистка папки dist
npm run clean
```

### Результаты сборки

После сборки файлы будут находиться в папке `dist/`:
- **Windows**: `.exe` установщик (NSIS) и portable версия
- **macOS**: `.dmg` файл
- **Linux**: `.AppImage` файл

## Использование

### Воспроизведение медиа

1. Нажмите кнопку "Открыть файл" или перетащите медиа-файл в окно приложения
2. Используйте стандартные элементы управления видео/аудио для управления воспроизведением
3. Для аудио файлов отображается визуализация звуковых волн

### Работа с субтитрами

Для видео файлов доступны следующие функции:

1. **Загрузка существующих субтитров**: Нажмите "Загрузить субтитры" и выберите файл SRT
2. **Создание новых субтитров**: Нажмите "Новые субтитры" для создания субтитров с нуля
3. **Редактирование субтитров**:
   - Нажмите "Редактор" чтобы открыть/скрыть редактор субтитров
   - Используйте "+ Добавить" для создания нового субтитра
   - Установите время начала и конца каждого субтитра, используя кнопки "Установить начало/конец" для привязки к текущей позиции видео
   - Введите текст субтитра в текстовое поле
   - Используйте кнопку "▶" для воспроизведения с начала субтитра
   - Удалите ненужные субтитры кнопкой "Удалить"
4. **Сохранение субтитров**: Нажмите "Сохранить субтитры" чтобы экспортировать субтитры в файл SRT
5. **Отображение субтитров**: Субтитры автоматически отображаются поверх видео во время воспроизведения

## Технологии

- **Electron** - кросс-платформенный фреймворк
- **HTML5 Video/Audio API** - для воспроизведения медиа
- **Vanilla JavaScript** - для логики приложения
- **CSS3** - для современного дизайна

## Лицензия

MIT
